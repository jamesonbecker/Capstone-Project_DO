import { SalesOrderDetails } from './sales-order-details';

describe('SalesOrderDetails', () => {
  it('should create an instance', () => {
    expect(new SalesOrderDetails()).toBeTruthy();
  });
});
