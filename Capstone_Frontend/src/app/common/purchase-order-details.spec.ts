import { PurchaseOrderDetails } from './purchase-order-details';

describe('PurchaseOrderDetails', () => {
  it('should create an instance', () => {
    expect(new PurchaseOrderDetails()).toBeTruthy();
  });
});
